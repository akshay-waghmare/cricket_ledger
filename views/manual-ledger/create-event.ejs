<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="mb-0">Create Offline Event</h1>
  <a href="/admin/manual-ledger" class="btn btn-secondary">Back</a>
</div>

<div class="card">
  <div class="card-body">
    <form action="/admin/manual-ledger/events" method="POST">
      <div class="mb-3">
        <label for="eventName" class="form-label">Event Name</label>
        <input
          type="text"
          class="form-control"
          id="eventName"
          name="eventName"
          required
          value="<%= formData?.eventName || '' %>"
          placeholder="RCB vs CSK - Offline Ledger"
        >
      </div>

      <div class="mb-3">
        <label for="teams" class="form-label">Teams (optional)</label>
        <input
          type="text"
          class="form-control"
          id="teams"
          name="teams"
          value="<%= formData?.teams || '' %>"
          placeholder="Royal Challengers Bengaluru, Chennai Super Kings"
        >
        <div class="form-text">Comma-separated. Useful for match-odds reference, but optional for pure session tracking.</div>
      </div>

      <div class="mb-3">
        <label for="note" class="form-label">Notes (optional)</label>
        <textarea class="form-control" id="note" name="note" rows="3" placeholder="Any offline context you want to keep"><%= formData?.note || '' %></textarea>
      </div>

      <div class="d-flex justify-content-end gap-2">
        <a href="/admin/manual-ledger" class="btn btn-light">Cancel</a>
        <button type="submit" class="btn btn-primary">Create Event</button>
      </div>
    </form>
  </div>
</div>
