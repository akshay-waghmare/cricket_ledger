version: '3.8'
services:
  cricket-ledger:
    build: .
    container_name: cricket-ledger-app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/dist
    environment:
      - NODE_ENV=production
    restart: unless-stopped
